apiVersion: v1
kind: Pod
metadata:
  name: result-pod
  labels:
    name: result-pod
    app: demo-voting-app
spec:
  containers:
  - name: result-pod
    image: docker/example-voting-app-result
    resources:
      limits:
        memory: "128Mi"
        cpu: "500m"
    ports:
      - containerPort: 80
